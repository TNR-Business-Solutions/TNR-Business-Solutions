# Backend Dockerfile for AI Chatbot
FROM python:3.10-slim
WORKDIR /app
COPY backend/ /app/backend/
RUN pip install -r /app/backend/requirements.txt
CMD ["uvicorn", "orchestrator.main:app", "--host", "0.0.0.0", "--port", "8000"]
